[project]
name = "ha-marstek-venus"
version = "0.5.2"
description = "Home Assistant integration for Marstek Venus battery system"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [
    {name = "arvdrpoo"}
]

# No dependencies needed for the test script - uses only Python stdlib
dependencies = []

[project.scripts]
test-marstek = "test_connection:main"

[project.optional-dependencies]
dev = [
    "homeassistant>=2024.1.0",
]
